# KaliGPT / HatsOff — pip dependencies
# Install: pip install -r requirements/pip-requirements.txt
# GTK/gi for the native HatsOff window comes from apt (python3-gi), not pip:
#   sudo apt install -y python3-gi python3-gi-cairo gir1.2-gtk-3.0 gir1.2-webkit2-4.1
# and use: python3 -m venv --system-site-packages .venv

# --- AI providers ---
openai
ollama
google-genai
litellm
cursor-sdk

# --- CLI / agents ---
rich
requests
newspaper3k
lxml_html_clean
beautifulsoup4
ddgs
curl_cffi
nodriver
pyvirtualdisplay
prompt_toolkit

# --- HatsOff desktop ---
flask
pywebview
