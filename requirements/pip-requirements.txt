# KaliGPT / HatsOff — pip dependencies
# Install: pip install -r requirements/pip-requirements.txt
# GTK/gi for the native HatsOff window on Linux comes from apt (python3-gi), not pip:
#   sudo apt install -y python3-gi python3-gi-cairo gir1.2-gtk-3.0 gir1.2-webkit2-4.1
#   python3 -m venv --system-site-packages .venv
# On Windows, pywebview uses Edge WebView2 (usually preinstalled on Win10/11).
#   powershell -ExecutionPolicy Bypass -File .\install.ps1

# --- AI providers ---
openai
ollama
google-genai
litellm
cursor-sdk

# --- CLI / agents ---
rich
requests
newspaper3k
lxml_html_clean
beautifulsoup4
ddgs
curl_cffi
nodriver
pyvirtualdisplay
prompt_toolkit

# --- HatsOff desktop ---
flask
pywebview
